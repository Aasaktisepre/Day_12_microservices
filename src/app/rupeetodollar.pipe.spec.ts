import { RupeetodollarPipe } from './rupeetodollar.pipe';

describe('RupeetodollarPipe', () => {
  it('create an instance', () => {
    const pipe = new RupeetodollarPipe();
    expect(pipe).toBeTruthy();
  });
});
