import { CharAtIndexPipe } from './char-at-index.pipe';

describe('CharAtIndexPipe', () => {
  it('create an instance', () => {
    const pipe = new CharAtIndexPipe();
    expect(pipe).toBeTruthy();
  });
});
