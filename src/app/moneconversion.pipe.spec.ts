import { MoneconversionPipe } from './moneconversion.pipe';

describe('MoneconversionPipe', () => {
  it('create an instance', () => {
    const pipe = new MoneconversionPipe();
    expect(pipe).toBeTruthy();
  });
});
